/**
 * ShopSpy Extension — Configuration
 *
 * Single source of truth for the backend URL.
 * Change this value when deploying to a different server.
 */
const SHOPSPY_CONFIG = {
  API_BASE: "https://shop-spy.onrender.com",
};
